pipeline {
    agent any
    stages {
        stage('Git Clone') {
            steps {
                script {
                    chattingoStages.gitClone()
                }
            }
        }
    }
}